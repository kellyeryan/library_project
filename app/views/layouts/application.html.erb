<!DOCTYPE html>
<html>
  <head>
    <title>Library</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application', media: 'all' %>

    <!-- Required meta tags always come first -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" integrity="sha384-HSMxcRTRxnN+Bdg0JdbxYKrThecOKuH5zCYotlSAcp1+c8xmyTe9GYg1l9a69psu" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <script src="https://kit.fontawesome.com/3cbf063335.js" crossorigin="anonymous"></script>

  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
      <div class="container-fluid">
        <% if !current_patron %>
          <a class="navbar-brand mr-auto" href="<%= root_url %>"/><span style=""><i class="fas fa-user-plus"> Login or Sign up Page</i></span></a>
        <% else %>
          <a class="navbar-brand mr-auto" id="<%= session[:patron_id] %>" href="<%= patron_path(session[:patron_id]) %>"/><span class="fas fa-book-reader"></span> Patron Account</a>
          <a class="navbar-brand" href="<%= logout_url %>"/><span class="fas fa-sign-out-alt"></span> Logout</a>
        <% end %>
      </div>
      <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">

        </ul>
      </div><!--/.nav-collapse -->
    </nav>

    <!-- content -->
    <div class="container"  role="main">
      <%= yield %>
    </div>
</body>
</html>
