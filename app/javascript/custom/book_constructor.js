class Book {
  constructor(title, last_name, first_name, genre) {
    // this.id = id
    this.title = title
    this.last_name = last_name
    this.first_name = first_name
    this.genre = genre
  }
}
